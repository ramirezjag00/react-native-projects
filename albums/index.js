import { AppRegistry } from 'react-native';
import App from './App';

//render it to the device using AppRegistry which took our app name albums when we create it
//register atleast one component or parent component 
AppRegistry.registerComponent('albums', () => App);
